# Plans

Execution plans generated by the `/create-plan` command. Files are named `YYYY-MM-DD-[slug].md` and tracked with status emojis and an overall progress percentage.

See `../../examples/plans/` for a sample plan in the expected format.
