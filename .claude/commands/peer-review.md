A different tool or model has reviewed the current code/implementation and provided findings below. Important context:

- **They have less context than you** on this project's architecture and design decisions.
- **You are the lead** — don't accept findings at face value.
- Your job is to critically evaluate each finding against this project's specific architecture and requirements.

Findings from peer review:

$ARGUMENTS

---

For EACH finding above:

1. **Verify it exists** — actually check the code, configs, and architecture docs. Does this issue really exist?
2. **If it doesn't exist** — explain clearly why (maybe it's already handled elsewhere, or they misunderstood the design)
3. **If it does exist** — assess severity:
   - **CRITICAL** — security breach, data leak, credential exposure
   - **HIGH** — bugs, missing error handling, data integrity risks
   - **MEDIUM** — code quality, maintainability, performance concerns
   - **LOW** — style, documentation, minor improvements

After analysis, provide:
- Summary of valid findings (confirmed issues) with severity
- Summary of invalid findings (with explanations of why they don't apply)
- Prioritized action plan for confirmed issues
- Any findings that reveal gaps in our documentation (if the reviewer misunderstood something, our docs may need clarification)
